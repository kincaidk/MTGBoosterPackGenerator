version: 2.1

workflows:
  build-test:
    jobs:
      - build

jobs:
  # Generates a booster
  build:
    docker:
      - image: cimg/node:16.17.0
    steps:
      - run:
          name: Test booster generation.
          command: |
            yarn install
            node MTGBoosterGenerator.js ELD
